using System.Collections.Generic;
using UnityEngine;

public class MissionController : MonoBehaviour
{
    public List<MissionsScriptableObject> activeMissionList;
    public MissionsScriptableObject[] availableEasyMissions;
    public MissionsScriptableObject[] availableNormalMissions;
    public MissionsScriptableObject[] availableHardMissions;
    public MissionsScriptableObject[] availableExtremeMissions;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
